/**
 * Product display brand. Package/repo stay `llm-prompt-studio` for installs and URLs.
 */
export const PRODUCT_NAME = 'Castcut';

/** Short pitch for metadata, PWA, and desktop. */
export const PRODUCT_TAGLINE = 'Local character films with ComfyUI — Cast → Moodboard → Day → Cut.';

/** Comfy SaveImage / output folder prefix (no spaces). */
export const PRODUCT_OUTPUT_PREFIX = 'Castcut';
